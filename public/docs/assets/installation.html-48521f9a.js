import{_ as e,p as t,q as o,a1 as i}from"./framework-d1b33ab5.js";const a={},n=i('<h1 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h1><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>Create a database first.</p></div><ol><li><p>Extract your downloaded code and switch to the extracted path.</p></li><li><p>Setup your <code>database</code> and add the related keys to the <code>.env</code> file.</p></li><li><p>Run <code>composer install</code> if its the initial setup.</p></li><li><p>Setup other environment keys in <code>.env</code></p><ul><li>Run first <code>php artisan key:generate</code></li><li><strong>Note</strong>: If .env does not exist then copy <code>.env.example</code> as <code>.env</code></li></ul></li><li><p>Run <code>php artisan migrate</code> for initial tables setup. (use <code>--seed</code>, to setup initial or test data)</p></li><li><p><strong>Optional:</strong> Run <code>php artisan db:seed --class=RoleTableSeeder</code> to set the initial roles and permissions, then follow <code>step 7</code> below to assign a user the initial permissions and roles. <strong>Skip if you seeded the database when initially migrating</strong></p></li><li><p><strong>Optional:</strong> To create a <code>root</code> admin; Run <code>php artisan role:assign youremail@example.org admin-root</code>. Substitute <code>youremail@example.org</code> with an existing user email. <code>admin-root</code> is the <strong>default root Admin role</strong>.</p><p><strong>Note:</strong> You must follow <code>step 5 and 6</code> above first to setup the root admin.</p></li></ol>',3),s=[n];function l(d,r){return t(),o("div",null,s)}const p=e(a,[["render",l],["__file","installation.html.vue"]]);export{p as default};
